import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    headerButton:{
        marginRight:16
    },
    headerButtonText:{
        color:'white',
        fontSize:28
    }
})

export default styles